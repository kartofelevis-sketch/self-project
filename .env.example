SECRET_KEY=change-me-to-something-random
# Flask-SQLAlchemy сам подставляет папку instance/ перед относительным путём —
# не пиши "instance/" сама, иначе получится instance/instance/self.db
DATABASE_URL=sqlite:///self.db
BOT_API_KEY=change-me-to-a-long-random-string
